import { MOCK_TEMPLATE_REGISTRY } from '../components/mockData';

export const TEMPLATE_REGISTRY = MOCK_TEMPLATE_REGISTRY;
export const TEMPLATE_LIST = Object.values(MOCK_TEMPLATE_REGISTRY);

// Backward compatibility (Types for reference if needed locally, though better in types.ts)
